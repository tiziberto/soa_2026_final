apiVersion: 1

providers:
  - name: "SOA Dashboards"
    orgId: 1
    folder: "SOA"
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
